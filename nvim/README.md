# Using only this repo

## Install neovim
```
sudo apt install nvim
```
## Configure nvim

Copy the content of this directory to `~/.config/nvim` and open `nvim` and install the plugins with: `:PlugInstall`

(ignore the errors, it's just temporary)
